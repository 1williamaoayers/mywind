# MyWind API 接口文档报告

> 生成时间：2025-12-28
> 
> 当前 API 服务地址：`http://localhost:3000`

---

## 📌 概述

MyWind 项目 **已实现对外 API 接入功能**，提供 RESTful 风格的接口服务。

| 项目 | 说明 |
|------|------|
| **协议** | HTTP (可配置 HTTPS) |
| **格式** | JSON |
| **版本** | v1.0.0 / v2.0.0 |
| **认证** | Bearer Token (部分接口需要) |

---

## 🏗️ API 架构

### 两套接口系统

```
┌─────────────────────────────────────────────────────┐
│                   MyWind API 服务                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌───────────────────────────────────────────┐      │
│  │ /api/v1/*  - 核心业务 API (server.js)     │      │
│  │  ├─ 用户认证 (auth)                       │      │
│  │  ├─ 行情数据 (market)                     │      │
│  │  ├─ 财务数据 (financial)                  │      │
│  │  ├─ 订阅管理 (subscriptions)              │      │
│  │  ├─ 报告生成 (reports)                    │      │
│  │  ├─ AI 问答 (ai/chat)                     │      │
│  │  └─ 系统状态 (status)                     │      │
│  └───────────────────────────────────────────┘      │
│                                                     │
│  ┌───────────────────────────────────────────┐      │
│  │ /api/*  - 业务扩展 API (routes/*.js)      │      │
│  │  ├─ 股票管理 (stocks)                     │      │
│  │  ├─ 新闻查询 (news)                       │      │
│  │  ├─ 飞书预警 (alerts)                     │      │
│  │  ├─ AI 研报 (reports)                     │      │
│  │  ├─ 调度管理 (scheduler)                  │      │
│  │  ├─ 系统配置 (config)                     │      │
│  │  ├─ 账号保险箱 (accounts)                 │      │
│  │  ├─ 视觉采集 (visual)                     │      │
│  │  ├─ 研报采集 (research)                   │      │
│  │  ├─ 数据源采集 (多个子路径)                │      │
│  │  └─ 港股专属 (hk) ✨                       │      │
│  └───────────────────────────────────────────┘      │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 📡 核心 API 列表 (`/api/v1/*`)

### 1️⃣ 用户认证

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | `/api/v1/auth/register` | 用户注册 | ❌ |
| POST | `/api/v1/auth/login` | 用户登录，返回 Token | ❌ |
| POST | `/api/v1/auth/logout` | 用户登出 | ✅ |
| GET | `/api/v1/auth/me` | 获取当前用户信息 | ✅ |

**示例：登录**
```bash
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "123456"}'
```

---

### 2️⃣ 行情数据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/market/indices` | 获取大盘指数行情 |
| GET | `/api/v1/market/quote/:code` | 获取单只股票行情 |
| GET | `/api/v1/market/sectors` | 获取板块行情 |

**示例：获取股票行情**
```bash
curl http://localhost:3000/api/v1/market/quote/00700.HK
```

---

### 3️⃣ 财务数据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/financial/:code/summary` | 获取财务摘要 |
| GET | `/api/v1/financial/:code/valuation` | 获取估值分析 |

---

### 4️⃣ 订阅管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/subscriptions` | 获取所有订阅 |
| POST | `/api/v1/subscriptions` | 添加订阅 |
| DELETE | `/api/v1/subscriptions/:code` | 删除订阅 |

---

### 5️⃣ 股票新闻

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/stocks/:code/news` | 获取指定股票的新闻 |

---

### 6️⃣ 报告生成

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/reports/daily` | 获取每日报告列表 |
| GET | `/api/v1/reports/daily?date=YYYY-MM-DD` | 获取指定日期报告 |
| POST | `/api/v1/reports/generate` | 生成每日报告 |

---

### 7️⃣ AI 问答

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/ai/chat` | AI 智能问答 |

**示例：AI 问答**
```bash
curl -X POST http://localhost:3000/api/v1/ai/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "腾讯控股最近有什么重大新闻？"}'
```

---

### 8️⃣ 系统状态

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/status` | 获取系统运行状态 |

---

## 📡 业务扩展 API (`/api/*`)

### 🔔 健康检查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 服务健康检查 |

---

### 📰 新闻查询 (`/api/news`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/news` | 万得式新闻查询（支持多条件筛选） |
| GET | `/api/news/stats` | 今日新闻统计 |

**支持的查询参数：**
- `level` - 预警等级：red/green/blue
- `keyword` - 关键词搜索
- `source` - 来源筛选
- `dimension` - 维度筛选
- `stockId` - 股票ID筛选
- `startDate/endDate` - 日期范围
- `limit/page` - 分页

**示例：查询利空新闻**
```bash
curl "http://localhost:3000/api/news?level=red&limit=20"
```

---

### 📊 股票管理 (`/api/stocks`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stocks` | 获取所有股票 |
| POST | `/api/stocks` | 添加股票 |
| PUT | `/api/stocks/:id/matrix` | 更新关键词矩阵 |
| DELETE | `/api/stocks/:id` | 删除股票 |

---

### 📝 AI 研报 (`/api/reports`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/reports` | 获取研报列表 |
| GET | `/api/reports/stats/tokens` | Token 使用统计 |
| GET | `/api/reports/:id` | 获取研报详情 |
| POST | `/api/reports/generate` | 生成单只股票研报 |
| POST | `/api/reports/generate-all` | 批量生成所有研报 |
| POST | `/api/reports/:id/send-feishu` | 发送研报到飞书 |

---

### 🔬 研报采集 (`/api/research`)

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/research/status` | 研报源状态 |
| POST | `/api/research/scrape` | 触发多源研报采集 |
| GET | `/api/research/search?keyword=xxx` | 搜索研报 |
| GET | `/api/research/latest` | 获取最新研报 |
| GET | `/api/research/stats` | 研报统计 |

---

### 🇭🇰 港股专属 (`/api/hk`) ⭐

#### 调度管理
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/hk/scheduler/status` | 港股调度器状态 |
| POST | `/api/hk/scheduler/start` | 启动调度器 |
| POST | `/api/hk/scheduler/stop` | 停止调度器 |
| POST | `/api/hk/scheduler/run` | 手动触发采集 |

#### 港股资讯
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/hk/news/aastocks` | AAStocks 新闻 |
| GET | `/api/hk/news/zhitong` | 智通财经新闻 |
| GET | `/api/hk/news/etnet` | 经济通新闻 |
| GET | `/api/hk/news/hkej` | 信报财经新闻 |
| GET | `/api/hk/news/hket` | 香港经济日报 |

#### 港交所官方
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/hk/hkex/news` | 港交所新闻 |
| GET | `/api/hk/hkex/announcements` | 港交所公告 |
| GET | `/api/hk/hkex/ipo` | 港股 IPO |

#### 港股通资金
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/hk/northbound/flow` | 资金流向 |
| GET | `/api/hk/northbound/top10` | 十大成交 |
| GET | `/api/hk/northbound/history` | 历史数据 |
| GET | `/api/hk/northbound/holding/:stockCode` | 个股持仓 |

#### 港股研报
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/hk/research/aastocks` | AAStocks 研报 |
| GET | `/api/hk/research/hkej-column` | 信报专栏 |
| GET | `/api/hk/research/zhitong` | 智通财经研报 |

#### 窝轮牛熊
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/hk/warrant` | 窝轮排行 |
| GET | `/api/hk/cbbc` | 牛熊证排行 |

---

### 🕷️ 数据源采集 (`/api/*`)

#### 政策哨兵
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/policy/status` | 政策哨兵状态 |
| POST | `/api/policy/check` | 触发政策检查 |

#### 另类数据
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/altdata/status` | 另类数据状态 |
| POST | `/api/altdata/fetch` | 采集另类数据 |
| GET | `/api/altdata/latest` | 最新另类数据 |
| GET | `/api/altdata/chart/:indicator` | 图表数据 |

#### 各数据源采集
| 数据源 | 状态接口 | 采集接口 |
|--------|----------|----------|
| 腾讯财经 | GET `/api/tencent/status` | POST `/api/tencent/fetch` |
| 格隆汇 | GET `/api/gelonghui/status` | POST `/api/gelonghui/fetch` |
| 金十数据 | GET `/api/jin10/status` | POST `/api/jin10/fetch` |
| 国家统计局 | GET `/api/stats/status` | POST `/api/stats/fetch` |
| 36氪 | GET `/api/36kr/status` | POST `/api/36kr/fetch` |
| 集微网 | GET `/api/jimei/status` | POST `/api/jimei/fetch` |
| 富途牛牛 | GET `/api/futu/status` | POST `/api/futu/fetch` |
| 知乎财经 | GET `/api/zhihu/status` | POST `/api/zhihu/fetch` |
| 全球媒体 | GET `/api/global/status` | POST `/api/global/fetch` |

---

## 📋 统一响应格式

所有接口采用统一的 JSON 响应格式：

### 成功响应
```json
{
  "success": true,
  "data": { ... },
  "pagination": {     // 仅列表接口
    "total": 100,
    "page": 1,
    "limit": 20,
    "pages": 5
  }
}
```

### 错误响应
```json
{
  "success": false,
  "error": {
    "message": "错误描述"
  }
}
```

---

## 🔐 认证方式

需要认证的接口，请在请求头添加：

```
Authorization: Bearer <token>
```

获取 Token：
```bash
# 登录获取 token
curl -X POST http://localhost:3000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "123456"}'

# 返回
{
  "success": true,
  "data": {
    "user": { ... },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

---

## 🔗 用户如何知道 API 链接

### 方案一：服务器启动提示
服务器启动时会显示：
```
╔════════════════════════════════════════════╗
║                                            ║
║   📊 MyWind 智能投研终端                   ║
║                                            ║
║   🌐 访问地址: http://localhost:3000       ║
║                                            ║
║   人人都买得起的万得终端                   ║
║                                            ║
╚════════════════════════════════════════════╝
```

### 方案二：健康检查接口
访问 `http://localhost:3000/api/health` 可验证服务状态

### 方案三：本文档
将本文档放置在项目根目录，用户可直接查阅

---

## 📊 API 统计

| 类别 | 接口数量 |
|------|----------|
| 核心 API (`/api/v1/*`) | 17 个 |
| 新闻查询 | 2 个 |
| 股票管理 | 4 个 |
| AI 研报 | 6 个 |
| 研报采集 | 5 个 |
| 港股专属 | 22 个 |
| 数据源采集 | 26 个 |
| **总计** | **82+ 个** |

---

## ⚠️ 当前限制

1. **无 API 文档页面**：建议添加 Swagger/OpenAPI 文档
2. **无限流控制**：建议添加请求频率限制
3. **无 API Key 管理**：目前仅支持用户 Token 认证
4. **无 WebSocket**：实时推送功能待实现

---

## 🚀 推荐改进

1. **添加 Swagger UI**：自动生成 API 文档页面
2. **API Key 系统**：支持第三方接入
3. **限流保护**：防止恶意请求
4. **WebSocket 支持**：实时行情推送
5. **API 网关**：统一入口管理

---

*报告完成 ✅*
