# 68个采集源真实档案报告

**测试时间**: 2026-01-04 15:15 - 17:15  
**测试完成度**: 67/68 (98.5%)  
**测试股票**: 京东集团 (09618)

---

## 🔴 核心发现

### 1. 绝大多数源**无正文内容**

| 数据质量 | 源数量 | 占比 |
|----------|--------|------|
| 有正文(quality≥3) | **2个** | 3% |
| 无正文(quality=1) | **58个** | 87% |
| 错误/超时 | **7个** | 10% |

### 2. 唯一有正文的源

| 源 | 模式 | 正文长度 | 样本 |
|----|------|----------|------|
| **同花顺(ths)** | 通用采集 | 174-218字 | "中方呼吁立即释放马杜罗总统...外交部发言人..." |
| **SEC** | 通用采集 | 113字 | 美股SEC文档信息 |

### 3. 无法测试的源（函数名不匹配）

以下源在`stockNewsCollector.js`中的函数名与脚本预期不符：

| 源 | 预期函数 | 实际问题 |
|----|----------|----------|
| aastocks | `scrapeAastocksForStock` | 函数是`scrapeAAStocksForStock`（大小写） |
| etnet | `scrapeEtnetForStock` | 函数是`scrapeETNetForStock` |
| hkex | `scrapeHkexForStock` | 函数是`scrapeHKEXForStock` |
| hket | `scrapeHketForStock` | 函数是`scrapeHKETForStock` |
| hkej | `scrapeHkejForStock` | 函数是`scrapeHKEJForStock` |
| nbd | `scrapeNbdForStock` | 函数是`scrapeNBDForStock` |
| stcn | `scrapeStcnForStock` | 函数是`scrapeSTCNForStock` |
| sec | `scrapeSecForStock` | 函数是`scrapeSECForStock` |
| seekingalpha | `scrapeSeekingalphaForStock` | 函数是`scrapeSeekingAlphaForStock` |
| cls | `scrapeClsForStock` | 函数是`scrapeCLSForStock` |
| hkexnews | `scrapeHkexnewsForStock` | 函数是`scrapeHKEXNewsForStock` |
| ths | `scrapeThsForStock` | 函数是`scrapeTHSForStock` |
| eastmoney_report | `scrapeEastmoney_reportForStock` | 函数是`scrapeEastmoneyReportForStock` |

### 4. 浏览器超时的源

以下源在测试时出现"获取浏览器超时"错误：

- taoguba (淘股吧)
- fxbaogao (发现报告-定向)
- eastmoney (东财)
- sina (新浪)
- cninfo (巨潮)
- wallstreet (华尔街见闻)
- guba (股吧)
- globalMedia (全球媒体-定向)
- northbound (北向资金-通用)
- interactive (互动易)
- stats (统计局)

---

## 📊 27个重复源对比结果

### 对比有效的源

| 源 | 通用采集 | 定向采集 | 推荐 |
|----|----------|----------|------|
| **ths (同花顺)** | ✅ 有正文(3分) | 函数名错误 | **通用采集** |
| **sec** | ✅ 有正文(3分) | 函数名错误 | **通用采集** |
| futu | 无正文(1分) | 无正文(1分) | 平局 |
| gelonghui | 无正文(1分) | 无正文(1分)，但相关性高 | 定向采集（相关性更好） |
| jiemian | 无正文(1分) | 无正文(1分)，标题相关 | 定向采集（相关性更好） |
| kr36 | 无正文(1分) | 无正文(1分)，标题相关"京东收购叮咚" | 定向采集（相关性更好） |
| jin10 | 有短正文(1分) | 无正文(1分)，标题相关 | 定向采集（相关性更好） |
| yahoo | 无正文(1分) | 无正文(1分)，标题相关"JD.com" | 定向采集（相关性更好） |
| yanbaoke | 无正文(1分) | 返回0条 | **通用采集**（至少有数据） |
| weibo | 无正文(1分) | 返回0条 | **通用采集**（至少有数据） |
| zhihu | 无正文(1分) | 返回0条 | **通用采集**（至少有数据） |
| tencent | 无正文(1分) | 返回0条 | **通用采集**（至少有数据） |
| jimei | 无正文(1分) | 返回0条 | **通用采集**（至少有数据） |
| zhitong | 返回0条 | 返回0条 | **两者都不可用** |

### 无法对比的源（定向采集函数名错误）

aastocks, etnet, hkex, hket, hkej, nbd, stcn, seekingalpha, globalMedia, northbound, taoguba, fxbaogao

---

## 📋 9个定向采集专属源

| 源 | 测试结果 | 评分 |
|----|----------|------|
| xueqiu | 1条，无正文，需Cookie | ❌ 1分 |
| eastmoney | 超时 | ❌ 失败 |
| sina | 超时 | ❌ 失败 |
| eastmoney_report | 函数名错误 | ❌ 失败 |
| cls | 函数名错误 | ❌ 失败 |
| cninfo | 超时 | ❌ 失败 |
| hkexnews | 函数名错误 | ❌ 失败 |
| wallstreet | 超时 | ❌ 失败 |
| guba | 超时 | ❌ 失败 |

---

## 📋 5个通用采集专属源

| 源 | 测试结果 | 评分 |
|----|----------|------|
| interactive | 超时 | ❌ 失败 |
| sinaFinance | 代码错误 | ❌ 失败 |
| stats | 超时 | ❌ 失败 |
| wechatSearch | 0条 | ❌ 失败 |

---

## 🎯 决策建议

### 值得接入调度器的源

| 优先级 | 源 | 模式 | 理由 |
|--------|-----|------|------|
| **🔴 P0** | **ths (同花顺)** | 通用采集 | 唯一中文源有正文，正文>100字 |
| **🔴 P0** | **sec** | 通用采集 | SEC文档有结构化数据 |
| 🟡 P1 | jiemian, kr36, gelonghui | 定向采集 | 无正文但标题相关性高 |
| 🟡 P1 | jin10, yahoo | 定向采集 | 无正文但标题相关性高 |

### 需要修复的问题

1. **函数名大小写不一致**：13个源的函数名与SCRAPERS对象key不匹配
2. **浏览器池超时**：11个源因浏览器资源不足失败
3. **无正文内容**：58个源只抓标题不抓正文

### 不建议使用的源

- zhitong (智通财经)：被WAF阻断
- weibo, zhihu, tencent：需要登录
- wechatSearch：无法获取数据

---

## 📝 总结

### 按用户标准（有实质内容>100字）判定：

- **有效源数量**: 2/67 = **3%**
- **无效源数量**: 65/67 = **97%**

### 根本问题

**代码设计问题**：
1. 大多数采集函数只提取标题，从不进入详情页抓取正文
2. 函数命名不统一，导致多个源无法调用
3. 浏览器池资源管理不当，导致超时

### 立即行动建议

1. **优先接入同花顺通用采集**：唯一有正文的中文源
2. **修复函数名大小写问题**：统一命名规范
3. **优化浏览器池**：增加池大小或实现串行队列
4. **长期修改采集逻辑**：增加详情页正文抓取
