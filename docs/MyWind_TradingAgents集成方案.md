# MyWind + TradingAgents 集成方案

> **创建日期**: 2026-01-06  
> **目标**: 让MyWind作为数据中台，为TradingAgents-arm32提供完整的AkShare数据支持

---

## 📋 项目关系分析

### TradingAgents-arm32 是什么？

**定位**: AI驱动的股票分析助手（前端应用）

**架构**:
```
┌─────────────────────────────────────────┐
│      Streamlit Web界面                   │
│  (配置向导 + 分析表单 + 结果展示)         │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│       AI多智能体系统                     │
│  ┌──────────┐  ┌──────────┐            │
│  │市场分析师│  │新闻分析师│            │
│  └──────────┘  └──────────┘            │
│  ┌──────────┐  ┌──────────┐            │
│  │基本面分析│  │风险评估  │            │
│  └──────────┘  └──────────┘            │
└─────────────────┬───────────────────────┘
                  │
┌─────────────────▼───────────────────────┐
│            数据层                        │
│  ┌──────────┐  ┌──────────┐            │
│  │新闻聚合  │  │市场数据  │            │
│  └──────────┘  └──────────┘            │
│  ┌──────────┐  ┌──────────┐            │
│  │MongoDB   │  │Redis缓存 │            │
│  └──────────┘  └──────────┘            │
└─────────────────────────────────────────┘
```

**当前数据源**:
- FinnHub - 美股/港股新闻
- Alpha Vantage - 美股数据
- **AKShare - A股数据** ⭐
- 财联社 - 中文财经新闻
- Yahoo Finance - 全球市场数据

**四大AI分析师**:
1. **市场分析师** - 技术分析（K线、均线、MACD、RSI）
2. **新闻分析师** - 实时新闻、舆情分析
3. **基本面分析师** - 财务数据、估值分析
4. **风险评估师** - 多角度风险评估

---

### MyWind 是什么？

**定位**: 金融数据中台（后端数据服务）

**核心价值**:
- 数据采集与清洗
- 统一数据格式
- MongoDB存储
- ChromaDB向量检索
- RESTful API对外服务

**当前问题**:
- ❌ 依赖40+ Puppeteer爬虫，内存占用高、不稳定
- ❌ 港股数据覆盖不足
- ❌ 数据结构不统一

---

## 🎯 集成目标

### 核心思路

**让MyWind成为TradingAgents的"数据底座"**:

```
┌──────────────────────────────────────────────────────┐
│         TradingAgents-arm32 (前端)                    │
│      Streamlit + AI多智能体系统                        │
└────────────────────┬─────────────────────────────────┘
                     │ HTTP API
                     ▼
┌──────────────────────────────────────────────────────┐
│            MyWind 数据中台 (新架构)                    │
│  ┌────────────────────────────────────────────────┐  │
│  │   Python FastAPI Gateway (新增)                │  │
│  │    150+ AkShare接口封装                        │  │
│  └────────────────┬───────────────────────────────┘  │
│                   │                                   │
│  ┌────────────────▼───────────────────────────────┐  │
│  │   Node.js 服务 (保留)                          │  │
│  │    Puppeteer爬虫 (仅保留AkShare无法覆盖的源)   │  │
│  └────────────────┬───────────────────────────────┘  │
│                   │                                   │
│  ┌────────────────▼───────────────────────────────┐  │
│  │   MongoDB + ChromaDB + Redis                   │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

### 具体目标

1. **替代TradingAgents的AKShare直接调用**
   - TradingAgents目前直接调用AKShare**核心价值**:
1. **TradingAgents的数据底座** - 提供稳定、完整、实时的中国+港股+美股+宏观数据
2. **为五大分析师提供完整数据** ⭐ 更新
   - Market Analyst (市场分析师) - 60个接口
   - News Analyst (新闻分析师) - 37个接口
   - Fundamental Analyst (基本面分析师) - 60个接口
   - Social Media Analyst (社交媒体分析师) - 10个接口 ⭐ 新增
   - Risk Assessment (风险与宏观分析师) - 86个接口
3. **替代不稳定的Web爬虫** - 用AkShare的稳定API替代Puppeteer重度爬虫
4. **提供缓存和清洗** - Redis缓存 + 数据清洗 + 统一格式
5. **独立部署** - 独立服务器 + 独立MongoDB + 独立Redis> CSS选择器）

---

## 🏗️ 集成架构方案

### 方案一：API代理模式（推荐）⭐

**架构**:
```
TradingAgents → MyWind FastAPI → AkShare Library
                      ↓
                  MongoDB缓存
```

**优势**:
- ✅ 最小改动：TradingAgents只需改API端点
- ✅ 统一缓存：MyWind提供Redis缓存层
- ✅ 数据清洗：MyWind统一数据格式
- ✅ 可监控：所有数据请求可追踪

**实施步骤**:
1.### 阶段2：AkShare接口实现 (Week 2-6) ⭐ 延长1周

1. 创建Python FastAPI项目结构
2. 封装253个AkShare接口为RESTful API ⭐ 更新接口数量
   - P0优先级：80个（第1-2周）
   - P1优先级：100个（第3-4周）
   - P2优先级：73个（第5+周）
3. 实现Redis分层缓存
4. 实现数据清洗和格式化
5. 实现错误处理和重试机制
6. 编写API文档（Swagger/OpenAPI）原始返回

---

### 方案二：共享MongoDB模式

**架构**:
```
TradingAgents → 直接读取MyWind的MongoDB
MyWind → 定时采集并写入MongoDB
```

**优势**:
- ✅ 低延迟：直接读数据库
- ✅ 离线可用：预采集数据

**劣势**:
- ❌ 紧耦合：两个项目共享数据库
- ❌ 格式依赖强

**不推荐理由**：违反微服务原则

---

## 📊 五大分析师数据需求映射 ⭐ 更新

### 1. 市场分析师 → 技术分析师接口（50个）

| TradingAgents需求 | MyWind提供的AkShare接口 |
|-------------------|------------------------|
| 港股实时行情 | `stock_hk_spot_em()` |
| 港股历史K线 | `stock_hk_hist()` |
| 恒生指数 | `stock_hk_index_daily_em()` |
| 技术指标数据 | `stock_rank_cxg_em()` (创新高) |
| 涨停板数据 | `stock_zt_pool_em()` |

### 2. 新闻分析师 → 新闻分析师接口（42个）

| TradingAgents需求 | MyWind提供的AkShare接口 |
|-------------------|------------------------|
| 实时快讯 | `stock_info_global_cls()` (财联社) |
| 个股新闻 | `stock_news_em()` |
| 港股公告 | `stock_hk_gg_em()` |
| 研报评级 | `stock_hk_analyst_estimate_em()` |
| 舆情热度 | `baidu_search_index()` |

### 3. 基本面分析师 → 基本面分析师接口（35个）

| TradingAgents需求 | MyWind提供的AkShare接口 |
|-------------------|------------------------|
| 财务报表 | `stock_financial_hk_report_em()` |
| 财务指标 | `stock_financial_hk_analysis_indicator_em()` |
| 分红配股 | `stock_hk_fhpx_em()` |
| 估值数据 | `stock_hk_valuation_baidu()` |
| 北向资金 | `stock_hsgt_individual_em()` |

### 4. 风险评估师 → 宏观分析师接口（23个）

| TradingAgents需求 | MyWind提供的AkShare接口 |
|-------------------|------------------------|
| 宏观经济 | `macro_china_gdp_yearly()` |
| 利率数据 | `macro_china_lpr()` |
| 汇率数据 | `forex_spot_em()` |
| 美联储利率 | `macro_bank_usa_interest_rate()` |
| 全球指数 | `index_global_hist_em()` |

---

## 🚀 实施路线图（4周）

### 第1周：基础设施搭建

**目标**: 创建MyWind的Python FastAPI服务

- [ ] 创建FastAPI项目结构
- [ ] Docker化部署
- [ ] 设计统一API响应格式
- [ ] 配置Redis缓存策略
- [ ] 编写API文档（Swagger）

### 第2周：核心接口实施（P0）

**目标**: 实现TradingAgents最需要的接口

**新闻分析师**（21个）:
- [ ] 快讯类6个
- [ ] 个股新闻4个
- [ ] 公告类5个
- [ ] 研报评级6个

**技术分析师**（20个）:
- [ ] 港股行情5个
- [ ] 指数数据8个
- [ ] 沪深港通5个
- [ ] 涨停板2个

### 第3周：补充接口（P1）

**基本面分析师**（20个）:
- [ ] 财务报表5个
- [ ] 财务指标6个
- [ ] 分红配股4个
- [ ] 估值数据5个

**宏观分析师**（15个）:
- [ ] 中国宏观8个
- [ ] 美国宏观5个
- [ ] 利率汇率2个

### 第4周：集成测试与上线

- [ ] TradingAgents配置修改
- [ ] 端到端测试（4只订阅股票）
- [ ] 性能测试与优化
- [ ] 监控告警配置
- [ ] 文档编写

---

## 🔌 API设计示例

### 统一端点格式

```
基础URL: http://mywind-api:5000/api/v1/akshare
```

### 分析师专属端点

#### 新闻分析师
```
GET /api/v1/akshare/news/flash          # 财联社快讯
GET /api/v1/akshare/news/stock/:symbol  # 个股新闻
GET /api/v1/akshare/news/announcement/:symbol  # 公告
GET /api/v1/akshare/news/research/:symbol      # 研报
```

#### 技术分析师
```
GET /api/v1/akshare/market/hk/quote          # 港股实时行情
GET /api/v1/akshare/market/hk/hist/:symbol   # 港股历史数据
GET /api/v1/akshare/market/index/:symbol     # 指数数据
GET /api/v1/akshare/market/hsgt/flow         # 北向资金
```

#### 基本面分析师
```
GET /api/v1/akshare/fundamental/report/:symbol    # 财务报表
GET /api/v1/akshare/fundamental/indicator/:symbol # 财务指标
GET /api/v1/akshare/fundamental/dividend/:symbol  # 分红数据
```

#### 宏观分析师
```
GET /api/v1/akshare/macro/china/gdp    # GDP数据
GET /api/v1/akshare/macro/usa/rate     # 美联储利率
GET /api/v1/akshare/macro/forex        # 外汇行情
```

### 响应格式

```json
{
  "success": true,
  "data": {
    // AkShare原始数据（保持格式兼容）
  },
  "metadata": {
    "source": "akshare",
    "cached": true,
    "timestamp": "2026-01-06T11:40:00Z"
  }
}
```

---

## ✅ 集成验证标准

### 功能验证
- [ ] 所有4个分析师都能获取到数据
- [ ] 数据格式与AkShare原始格式兼容
- [ ] 缓存策略生效（第二次请求<100ms）

### 性能验证
- [ ] API响应时间<2秒
- [ ] 并发100请求无报错
- [ ] MyWind内存占用<500MB

### 业务验证
- [ ] 使用小米(01810)测试完整分析流程
- [ ] 4个分析师输出质量符合预期
- [ ] 与直接调用AkShare对比无差异

---

## 📄 下一步行动

### 立即开始（推荐）

1. **创建Python FastAPI服务** → 搭建基础设施
2. **实现第1周核心接口** → 新闻+技术分析师数据
3. **TradingAgents配置调整** → 指向MyWind API
4. **端到端测试** → 用小米(01810)验证

### 需要用户确认的问题

1. **部署位置**：MyWind FastAPI服务部署在哪？（与TradingAgents同一台机器？）
2. **端口分配**：FastAPI用哪个端口？（建议5000）
3. **MongoDB共享**：是否复用TradingAgents的MongoDB？
4. **Redis共享**：是否复用TradingAgents的Redis？

---

**文档版本**: v1.0  
**创建时间**: 2026-01-06 11:40
