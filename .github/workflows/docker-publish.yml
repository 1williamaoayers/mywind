name: Build and Publish Multi-Arch Docker Image

on:
  push:
    branches: [main, master]
    paths-ignore:
      - '*.md'
      - 'docs/**'
  workflow_dispatch:  # 支持手动触发

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      # 1. 检出代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. 设置 QEMU (支持多架构模拟)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # 3. 设置 Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 4. 登录 GitHub Container Registry
      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 5. 提取元数据 (版本标签)
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha,prefix=
            type=ref,event=branch

      # 6. 构建并推送多架构镜像
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 7. 输出构建信息
      - name: Output image info
        run: |
          echo "=================================================="
          echo "🎉 多架构镜像构建成功！"
          echo "=================================================="
          echo ""
          echo "📦 镜像地址:"
          echo "   ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
          echo ""
          echo "🖥️ 支持平台:"
          echo "   - linux/amd64  (PC/服务器)"
          echo "   - linux/arm64  (Mac M系列/树莓派4)"
          echo "   - linux/arm/v7 (树莓派3/玩客云)"
          echo ""
          echo "🚀 一键运行命令:"
          echo "   docker run -d --name mywind-ai -p 8088:8088 \\"
          echo "     -e AI_API_KEY=你的Key \\"
          echo "     -e FEISHU_WEBHOOK=你的Webhook \\"
          echo "     ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
