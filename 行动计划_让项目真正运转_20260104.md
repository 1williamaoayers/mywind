# 行动计划：让项目真正运转起来

**日期**: 2026-01-04 11:45  
**当前状态**: 项目主服务未部署

---

 (主服务) | ❌ 容器不存在 |
| mywind-mongo (数据库) | ❌ 未运行 |
| mywind-ocr (OCR服务) | ✅ 运行中 |

---

## 二、我打算做的事

### 步骤1: 启动MongoDB容器
```bash
docker-compose up -d mongo## 一、当前真实状态

| 组件 | 状态 |
|------|------|
| mywind-app
```

### 步骤2: 修复server.js启动调度器
```javascript
// 在server.js中添加：
const { startScheduler } = require('./services/schedulerService');

app.listen(PORT, () => {
    console.log('MyWind 启动...');
    startScheduler();  // 启动定时采集任务
});
```

### 步骤3: 启动主服务
```bash
docker-compose up -d app
# 或本地运行:
node server.js
```

### 步骤4: 验证自动运转
- 检查调度器日志，确认定时任务在执行
- 等待5分钟，检查数据库是否有新数据入库
- 观察飞书是否收到推送

---

## 三、执行顺序

1. **立即执行**: 启动mongo容器
2. **修改代码**: 让server.js启动调度器
3. **启动服务**: 运行主服务
4. **持续监控**: 观察真实运转情况

---

## 四、预期结果

项目启动后，自动运转流程：

```
每5分钟 → 实时采集 → 入库MongoDB → 匹配订阅 → 飞书推送
每30分钟 → 深度采集
每2小时 → 政策哨兵
每天15:30 → 生成AI研报
```

---

**请确认后我立即执行。**
